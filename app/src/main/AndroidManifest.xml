<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rona.kumatraining6">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Beartivity" />
        <activity android:name=".Beartivity2" />
        <activity android:name=".MainBear">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Beartivity3" />
        <activity android:name=".MoveForResultBeartivity" />
        <activity android:name=".MoveWithBearDataBeartivity" />
        <activity android:name=".MoveWithBearBeartivity" />
        <activity android:name=".MoveBeartivity" />
    </application>

</manifest>